# sources of inspiration for this file
# * https://go2docs.graylog.org/5-1/downloading_and_installing_graylog/docker_installation.htm
# * https://github.com/Graylog2/docker-compose

version: '3'
services:

  # MongoDB: https://hub.docker.com/_/mongo/
  mongo:
    image: "mongo:6.0"
    container_name: mongo

  # OpenSearch:
  # * https://hub.docker.com/r/opensearchproject/opensearch
  # * https://opensearch.org/docs/1.3/install-and-configure/install-opensearch/docker/#sample-docker-composeyml
  opensearch:
    image: "opensearchproject/opensearch:1.3.9"
    container_name: opensearch1.3
    environment:
      - plugins.security.disabled=true
      - discovery.type=single-node
      - action.auto_create_index=false
      - bootstrap.memory_lock=true
    ulimits:
      memlock:
        soft: -1
        hard: -1

  # Graylog: https://hub.docker.com/r/graylog/graylog/
  graylog:
    image: "graylog/graylog:5.1.13"
    container_name: graylog
    links:
      - mongo:mongo
      - opensearch
    depends_on:
      - mongo
      - opensearch
    ports:
      # Graylog web interface and REST API
      - 9000:9000
      # Raw/Plaintext TCP
      - 5555:5555
      # Syslog TCP
      - 514:514
      # Syslog UDP
      - 514:514/udp
      # GELF TCP
      - 12201:12201
      # GELF UDP
      - 12201:12201/udp
    volumes:
     - ./graylog/config:/usr/share/graylog/data/config
     - ./graylog/plugin:/usr/share/graylog/plugin/
